{
  "name": "Python Flask Backend",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "settings": { 
    "terminal.integrated.shell.linux": "/bin/bash"
  },
  "extensions": ["ms-python.python"],
  "forwardPorts": [5000],
  "postCreateCommand": "echo 'Backend environment setup complete.'"
}
